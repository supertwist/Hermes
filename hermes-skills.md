# Hermes Skills – Expanded Descriptions

| Skill | Category | Description |
|-------|----------|-------------|
| `computer-use` |  | Drives the user’s desktop in the background: click, type, scroll, drag … without stealing the cursor or keyboard focus. Works on macOS, Windows and Linux, and can be used with any tool‑capable model. Load this skill whenever you need the `computer_use` tool. This skill is used when you need to computer use within Hermes workflows. |
| `dogfood` |  | Provides exploratory QA of web apps – automatically finds bugs, gathers evidence and creates a concise report. This skill is used when you need to dogfood within Hermes workflows. |
| `yuanbao` |  | Handles “Yuanbao” groups: lets you @‑mention users, query group members and retrieve basic info about the group. This skill is used when you need to yuanbao within Hermes workflows. |
| `apple-notes` | apple | Manages Apple Notes via the **memo** CLI – create new notes, search existing ones and edit their contents from the terminal. This skill is used when you need to apple notes within Hermes workflows. |
| `apple-reminders` | apple | Works with Apple Reminders through **remindctl** – add a reminder, list pending items or mark one as completed. This skill is used when you need to apple reminders within Hermes workflows. |
| `findmy` | apple | Tracks Apple devices and AirTags using the native FindMy.app on macOS; returns location information and battery status. This skill is used when you need to findmy within Hermes workflows. |
| `imessage` | apple | Sends and receives iMessages/SMS via the **imsg** CLI, supporting attachments and group chats. This skill is used when you need to imessage within Hermes workflows. |
| `macos-computer-use` | apple | Same as `computer-use` but pre‑configured for macOS: screenshots, mouse/keyboard control and drag‑and‑drop without stealing focus. This skill is used when you need to macos computer use within Hermes workflows. |
| `claude-code` | autonomous-ai-agents | Delegates coding tasks to the **Claude Code** CLI – create files, run tests or open PRs via Claude’s code‑generation backend. This skill is used when you need to claude code within Hermes workflows. |
| `codex` | autonomous-ai-agents | Uses the OpenAI **Codex** CLI for similar delegated coding: generate snippets, apply patches and manage pull requests. This skill is used when you need to codex within Hermes workflows. |
| `hermes-agent` | autonomous-ai-agents | Gives commands to configure, extend or contribute to Hermes Agent itself (e.g., change plugins, edit config files). This skill is used when you need to hermes agent within Hermes workflows. |
| `opencode` | autonomous-ai-agents | Sends coding work to the **OpenCode** CLI – useful for PR reviews and feature implementation via OpenAI’s code model. This skill is used when you need to opencode within Hermes workflows. |
| `architecture-diagram` | creative | Generates dark‑themed SVG architecture or cloud diagrams and returns them as embeddable HTML. This skill is used when you need to architecture diagram within Hermes workflows. |
| `ascii-art` | creative | Creates ASCII pictures using libraries like **pyfiglet**, **cowsay** or image‑to‑ASCII conversion. This skill is used when you need to ascii art within Hermes workflows. |
| `ascii-video` | creative | Converts video or audio into coloured ASCII MP4/GIF animations. This skill is used when you need to ascii video within Hermes workflows. |
| `baoyu-infographic` | creative | Produces infographics with 21 layouts and 21 visual styles (including Chinese “信息图”). This skill is used when you need to baoyu infographic within Hermes workflows. |
| `claude-design` | creative | Designs one‑off HTML artifacts such as landing pages, slide decks or quick prototypes using Claude’s design model. This skill is used when you need to claude design within Hermes workflows. |
| `comfyui` | creative | Installs and runs **ComfyUI** to generate images, video or audio; manages nodes, models and workflow parameters via the official CLI. This skill is used when you need to comfyui within Hermes workflows. |
| `design-md` | creative | Validates and exports Google DESIGN.md token specification files, ensuring they follow the required schema. This skill is used when you need to design md within Hermes workflows. |
| `excalidraw` | creative | Creates hand‑drawn style diagrams in Excalidraw JSON format (architecture, flow or sequence diagrams). This skill is used when you need to excalidraw within Hermes workflows. |
| `humanizer` | creative | Post‑processes generated text to strip AI‑like phrasing and add a natural human voice. This skill is used when you need to humanizer within Hermes workflows. |
| `manim-video` | creative | Produces math or algorithm animations using **Manim CE**, the same engine behind 3Blue1Brown videos. This skill is used when you need to manim video within Hermes workflows. |
| `p5js` | creative | Generates interactive sketches with **p5.js** – supports art, shaders and simple 3‑D scenes. This skill is used when you need to p5js within Hermes workflows. |
| `popular-web-designs` | creative | Provides ready‑made HTML/CSS implementations of 54 real design systems (Stripe, Linear, Vercel, …). This skill is used when you need to popular web designs within Hermes workflows. |
| `pretext` | creative | Builds DOM‑free HTML demos using **@chenglou/pretext** – ideal for ASCII art, kinetic typography and text‑driven generative graphics. This skill is used when you need to pretext within Hermes workflows. |
| `sketch` | creative | Quickly drafts throwaway HTML mockups; creates 2–3 design variants for fast comparison. This skill is used when you need to sketch within Hermes workflows. |
| `songwriting-and-ai-music` | creative | Guides the user through songwriting and generates music prompts compatible with Suno AI’s music models. This skill is used when you need to songwriting and ai music within Hermes workflows. |
| `touchdesigner-mcp` | creative | Controls a running **TouchDesigner** instance via the twozero MCP – create operators, set parameters, wire nodes and run Python scripts for live visuals. This skill is used when you need to touchdesigner mcp within Hermes workflows. |
| `jupyter-live-kernel` | data‑science | Opens an interactive Python session backed by a live Jupyter kernel (hamelnb), enabling step‑by‑step code execution. This skill is used when you need to jupyter live kernel within Hermes workflows. |
| `kanban-orchestrator` | devops | Provides a playbook and anti‑temptation rules for the orchestrator role in Hermes Kanban, guiding how to split work into subtasks. This skill is used when you need to kanban orchestrator within Hermes workflows. |
| `kanban-worker` | devops | Documents pitfalls, examples and edge cases for Kanban workers; explains the built‑in KANBAN_GUIDANCE lifecycle. This skill is used when you need to kanban worker within Hermes workflows. |
| `himalaya` | email | Command‑line email client (**Himalaya**) for IMAP/SMTP access – read, send and search mail without a GUI. This skill is used when you need to himalaya within Hermes workflows. |
| `codebase-inspection` | github | Uses **pygount** to report LOC, language breakdowns and other statistics for a repository. This skill is used when you need to codebase inspection within Hermes workflows. |
| `github-auth` | github | Walkthrough for setting up GitHub authentication: HTTPS tokens, SSH keys or logging in with the `gh` CLI. This skill is used when you need to github auth within Hermes workflows. |
| `github-code-review` | github | Automates PR reviews – fetches diffs, adds inline comments and can approve/merge via the `gh` CLI or REST API. This skill is used when you need to github code review within Hermes workflows. |
| `github-issues` | github | Creates, labels, assigns and triages GitHub issues using the `gh` CLI. This skill is used when you need to github issues within Hermes workflows. |
| `github-pr-workflow` | github | Manages the full PR lifecycle – branch creation, commits, opening a PR, waiting for CI and merging. This skill is used when you need to github pr workflow within Hermes workflows. |
| `github-repo-management` | github | Clones, forks or creates repositories; handles remotes, releases and repository settings. This skill is used when you need to github repo management within Hermes workflows. |
| `gif-search` | media | Searches Tenor for GIFs via **curl** + **jq**, then downloads the chosen result. This skill is used when you need to gif search within Hermes workflows. |
| `heartmula` | media | Generates songs with Suno‑like quality from lyrics and tags using the HeartMuLa service. This skill is used when you need to heartmula within Hermes workflows. |
| `image-gen` | media | Text‑to‑image generation tool (default backend **FAL.ai**) – pick a model, send a prompt and receive an image file. This skill is used when you need to image gen within Hermes workflows. |
| `songsee` | media | Extracts audio features such as mel spectrograms, chroma or MFCCs from a sound file via the CLI. This skill is used when you need to songsee within Hermes workflows. |
| `youtube-content` | media | Downloads a YouTube video’s transcript, then summarizes it or turns it into a threaded discussion or blog post. This skill is used when you need to youtube content within Hermes workflows. |
| `audiocraft-audio-generation` | mlops | Uses **AudioCraft** to generate music (MusicGen) or sound effects (AudioGen) from textual prompts. This skill is used when you need to audiocraft audio generation within Hermes workflows. |
| `evaluating-llms-harness` | mlops | Runs the **lm‑eval‑harness** suite to benchmark LLMs on tasks like MMLU, GSM8K and more. This skill is used when you need to evaluating llms harness within Hermes workflows. |
| `huggingface-hub` | mlops | Interacts with the HuggingFace hub via its CLI – search, download or upload models and datasets. This skill is used when you need to huggingface hub within Hermes workflows. |
| `llama-cpp` | mlops | Provides local inference for GGUF‑format Llama models using **llama.cpp**, plus model discovery on the HF hub. This skill is used when you need to llama cpp within Hermes workflows. |
| `segment-anything-model` | mlops | Runs Meta’s **SAM** to segment objects in an image given points, boxes or masks. This skill is used when you need to segment anything model within Hermes workflows. |
| `serving-llms-vllm` | mlops | Deploys high‑throughput LLM servers with **vLLM**, exposing an OpenAI‑compatible REST endpoint. This skill is used when you need to serving llms vllm within Hermes workflows. |
| `weights-and-biases` | mlops | Logs experiments, runs sweeps and visualises results via the **W&B** platform. This skill is used when you need to weights and biases within Hermes workflows. |
| `obsidian` | note‑taking | Reads, searches, creates and edits notes in the local Obsidian vault. This skill is used when you need to obsidian within Hermes workflows. |
| `airtable` | productivity | Performs CRUD operations on Airtable bases through its REST API (via **curl**). This skill is used when you need to airtable within Hermes workflows. |
| `google-workspace` | productivity | Accesses Gmail, Calendar, Drive, Docs and Sheets via the **gws** CLI or a Python SDK. This skill is used when you need to google workspace within Hermes workflows. |
| `maps` | productivity | Geocodes addresses, finds POIs, calculates routes and gets time‑zone data using OpenStreetMap/OSRM services. This skill is used when you need to maps within Hermes workflows. |
| `nano-pdf` | productivity | Edits PDF text, corrects typos or changes titles through the **nano-pdf** CLI with natural‑language prompts. This skill is used when you need to nano pdf within Hermes workflows. |
| `notion` | productivity | Works with Notion pages and databases via the **ntn** CLI – create, update, query and export markdown. This skill is used when you need to notion within Hermes workflows. |
| `ocr-and-documents` | productivity | Extracts text from PDFs or scanned images using **pymupdf** or **marker‑pdf**, returning plain text for further processing. This skill is used when you need to ocr and documents within Hermes workflows. |
| `petdex` | productivity | Installs animated “Petdex” mascots that run in the background of Hermes, adding a fun visual companion. This skill is used when you need to petdex within Hermes workflows. |
| `powerpoint` | productivity | Creates, reads and edits PowerPoint (`.pptx`) decks – add slides, update text or replace images programmatically. This skill is used when you need to powerpoint within Hermes workflows. |
| `teams-meeting-pipeline` | productivity | Runs the Teams‑meeting summarisation pipeline: records audio, transcribes, extracts action items and posts a summary back to Teams. This skill is used when you need to teams meeting pipeline within Hermes workflows. |
| `arxiv` | research | Searches arXiv for papers by keyword, author or category; can retrieve PDFs directly. This skill is used when you need to arxiv within Hermes workflows. |
| `blogwatcher` | research | Monitors blogs or RSS/Atom feeds, notifying when new entries appear that match a query. This skill is used when you need to blogwatcher within Hermes workflows. |
| `llm-wiki` | research | Builds and queries an interlinked markdown knowledge base (Karpathy’s LLM Wiki). This skill is used when you need to llm wiki within Hermes workflows. |
| `polymarket` | research | Queries the Polymarket API for market listings, current prices and historical order‑book data. This skill is used when you need to polymarket within Hermes workflows. |
| `source-table-compilation` | research | Provides a procedure to gather authoritative source tables with reference tracking and citation extraction. This skill is used when you need to source table compilation within Hermes workflows. |
| `openhue` | smart‑home | Controls Philips Hue lights, scenes and rooms via the **OpenHue** CLI. This skill is used when you need to openhue within Hermes workflows. |
| `xurl` | social‑media | Interacts with X/Twitter via the **xurl** CLI – post tweets, search timelines or send DMs. This skill is used when you need to xurl within Hermes workflows. |
| `browser-cdp` | software‑development | Gives raw access to the Chrome DevTools Protocol (CDP) for low‑level browser automation and debugging. This skill is used when you need to browser cdp within Hermes workflows. |
| `hermes-agent-skill-authoring` | software‑development | Guides creation of `SKILL.md` files: required front‑matter, validation rules and best‑practice writing tips. This skill is used when you need to hermes agent skill authoring within Hermes workflows. |
| `hermes-model-providers` | software‑development | Explains how to configure custom model providers (e.g., Ollama) in Hermes’ config file. This skill is used when you need to hermes model providers within Hermes workflows. |
| `node-inspect-debugger` | software‑development | Starts Node.js debugging with `--inspect` and connects via the Chrome DevTools Protocol CLI. This skill is used when you need to node inspect debugger within Hermes workflows. |
| `plan` | software‑development | Writes a one‑off actionable markdown plan to `.hermes/plans/`; does not execute anything automatically. This skill is used when you need to plan within Hermes workflows. |
| `python-debugpy` | software‑development | Launches a Python debug server using **debugpy**, enabling remote debugging from VS Code or other IDEs. This skill is used when you need to python debugpy within Hermes workflows. |
| `requesting-code-review` | software‑development | Performs pre‑commit security scans, quality checks and optional auto‑fix before opening a PR. This skill is used when you need to requesting code review within Hermes workflows. |
| `rhino-doc-search` | software‑development | Lightweight RAG search over locally saved Rhino 8 docs; loads all text files under `demo/rhino_docs` and returns the most relevant excerpts. This skill is used when you need to rhino doc search within Hermes workflows. |
| `rhino-mcp-helper` | software‑development | Translates natural‑language geometry requests into Rhino commands and forwards them to the **rhino3** MCP server. This skill is used when you need to rhino mcp helper within Hermes workflows. |
| `rhino-mcp-nl-helper` | software‑development | High‑level workflow for handling NL geometry requests: parses common shapes, falls back to `rhino-doc-search` if parsing fails, then returns instructions or results. This skill is used when you need to rhino mcp nl helper within Hermes workflows. |
| `rhino-mcp-research` | software‑development | Procedure for researching, validating and documenting MCP plugins for Rhino 8 – includes repo discovery, release checking and a markdown comparison table. This skill is used when you need to rhino mcp research within Hermes workflows. |
| `simplify-code` | software‑development | Runs three parallel agents that each clean up recent code changes (formatting, linting, static analysis). This skill is used when you need to simplify code within Hermes workflows. |
| `spike` | software‑development | Sets up a temporary sandbox to test an idea quickly before committing to a full implementation. This skill is used when you need to spike within Hermes workflows. |
| `systematic-debugging` | software‑development | Four‑phase root‑cause debugging framework: understand → reproduce → isolate → fix. This skill is used when you need to systematic debugging within Hermes workflows. |